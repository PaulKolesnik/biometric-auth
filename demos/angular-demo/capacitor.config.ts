import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.biometricauth.demo',
  appName: 'angularDemo',
  webDir: 'dist/angular-demo/browser'
};

export default config;
